var config = {
    // 要还原的代码路径
    file2decrypt: "./source.js",
    // 是否生成格式化后的代码，false为格式化，true为禁止格式化
    compact: false,
    // 要生成的代码路径
    file2generat: "./code.js",
}

module.exports = { config }
